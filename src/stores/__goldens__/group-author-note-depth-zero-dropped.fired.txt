# group/author-note-depth-zero-dropped   [SHOULD: pins the known #466 bug]
# what: A group author's note at depth 0 matches NEITHER branch (the loop
#   only reaches depthFromEnd >= 1, the overflow test is strictly >) and is
#   dropped. Solo emits it. Pinned deliberately so the future fix arrives as
#   a golden diff, not a surprise.
# pins: :2288-2306 loop, :2386-2395 overflow, comment at :2379-2385
# wiTimerOut.currentTurn (in): 1
# wiTimerOut.timers (in): (fresh chat — no seeded timers)
# toasts: (none raised by this build)
══════════════════════════════════════════════════════════════════════════
fired          = (none)
trimmedAtDepth = (none)
activated      = (none)
══════════════════════════════════════════════════════════════════════════
scanReport.budget           = 0
scanReport.pinnedTokens     = 0
scanReport.totalTokens      = 0
scanReport.pinnedOverBudget = false
scanReport.dropped          = (none)
