# Agent-team run ledger

One line per **pipeline exit** — deployed, parked, *or blocked*. Appended by the PM at `run-story` step 10c, before the run ends. Committed, so it survives the session that wrote it.

It exists because three things the pipeline depends on were not computable from any durable artifact:

1. **The CURATE trigger.** `run-story` fires the postmortem's staleness sweep when **exits since the last CURATE ≥ 5**. A fresh PM session has no other way to know the count — the Kanban records deployed *cards*, and blocked runs never reach it at all.
2. **Proof the postmortem ran.** A skipped stage and a clean one are otherwise byte-identical. The `verdict` + token columns are the evidence; a row with them blank is a skip, not a clean run. This holds for blocked exits too, which produce no close report and therefore no other proof.
3. **Postmortem cost.** Roadmap §5 bands cover build / verification / plan absorption. Postmortem is pipeline overhead with no band; this ledger is where its actuals accumulate until there are enough to set one.

**Never rewrite a past row.** Corrections are appended as a new row referencing the original. The ledger is evidence, and evidence that gets edited is not evidence.

## Columns

`date` · `story` · `exit` (deployed / parked / blocked) · `mode` (CAPTURE / CURATE) · `verdict` (the postmortem's verbatim one-liner) · `tokens` (build / verification / absorption / postmortem) · `notes` (pre-image snapshot paths, escalations, links)

| Date | Story | Exit | Mode | Postmortem verdict | Tokens (b/v/a/p) | Notes |
|---|---|---|---|---|---|---|
| 2026-08-24 | E1-S1 | deployed | — | *(pre-role — Pilot 1, no postmortem stage existed)* | — / ~1.3M / — / — | Retro lessons were hand-copied into 4 unsynced places; this is the failure that motivated the role |
| 2026-08-24 | E2-S1 | deployed | — | *(pre-role — Pilot 2, no postmortem stage existed)* | — / ~5.1M / ~4.5M / — | Same |
| 2026-08-26 | E9-S1 | blocked | CAPTURE | Intake's ground-truth rule worked — ~55k to kill a 4.5-month-stale card — but routing failed twice: both stale-card incidents corrected the roadmap and never the memory row that caused them (that file still holds 3 known-false rows), and the record's own "dev leg never ran" premise was itself stale. | 0 / 0 / 0 / ~1.56M | Blocked at INTAKE: deliverable shipped 2026-04-11 (PR #59). Postmortem ran via general-purpose fallback — the `postmortem` type merged in `882d8a4a` this session and types load at session start, so it was not registered. Postmortem ~103k + charter-§2 adjudication of its 7 proposals ~1.46M (17 agents: 7 refute-first verifiers, 9 red-team lenses, 1 completeness critic) — P6 REFUTED, 6 upheld-with-correction, and the critic found a 5th false row (10.2) the postmortem missed. **No pre-image was taken** for two memory edits this run made at INTAKE (the gate keyed on "postmortem proposal"); reconstructed at `.pre-image/E9-S1-reconstructed-2026-08-26.md`, gate fixed in this PR. Sammy then approved the full memory sweep — `project_roadmap_status.md` swept end-to-end (6 false rows corrected: 5.3/6.1/6.3/10.1/10.2/10.3, plus frontmatter, banner, the PR-#58 line and all 4 candidate items; row 6.4 re-verified accurate) + `MEMORY.md` lines 2 and 35, with timestamped snapshots taken first. Verdict column is the postmortem's verbatim line. |

| 2026-08-28 | E2-S2 | deployed | CAPTURE | The fix-round cascade is the house's real verification cost driver and nothing prices it: six rounds, three of which caught a defect the previous round's *fix* introduced, drove ~36.5M against an L verify band of 0.5–1.5M — while round 6 proved a converged round costs ~282k when scoped to the delta instead of ~1.9M; two new self-lying-test classes (assertions behind an always-false `if`; false `KILLS:` comments) and a recurrence of E1-S1's stray-worktree violation despite the rule being present and correctly duplicated in both stance copies. | ~6.9M (incl. ~0.8M intake + ~0.42M plan) / ~36.7M (6 adversarial rounds, 146 agents, 35 unique defects, 0 refuted + QA) / ~0.05M (light — code story; carrier sweep found+fixed stale memory `project_token_breakdown_analysis`) / ~0.13M | PRs #479/#481/#482 + backend #81 (prior sessions) + #484 (this run, merged 561dc842, deployed + prod-verified 2026-08-28, closes #453/#457). Pre-images: `.pre-image/project_token_breakdown_analysis.md.2026-08-28T012504.bak`, `.pre-image/MEMORY.md.2026-08-28T012504.bak`, `.pre-image/feedback_adversarial_review_catches_real_bugs.md.2026-08-28T013226.bak`. Proposals: B applied (additive); A+C await Sammy (removes_content); D/E/F (process rules) queued for design-mode red-team + follow-up PR. Escalations: §5 band model omits round-count term (flagged, not fixed — roadmap edit needs Sammy); watch-item: 0 refuted findings across 146 agents (second consecutive 0 becomes a finding); DECLINED.md does not exist yet. PLAN-leg pilot: artifact produced (§7.3 probation verdict remains Sammy's). This run also merged mid-flight process change #483 (board artifact) — gate+close refreshes done, PLAN-stage refresh predated the rule. CURATE counter: exit #2 since E9-S1. |

| 2026-08-28 | E2-S2 (follow-up to the row above) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — / — / — / ~3.4M design red-team + ~2.0M lost to a mid-run usage-limit kill (resumed from cache) | Sammy merged process PR #485 (postmortem D/E/F as amended by the design red-team: 77 agents, 3× UPHOLD WITH AMENDMENT — fix rounds keep FULL lenses over the fix delta, reduced lenses only on a confirmation round; `git status --porcelain` byte-clean gate every round; rounds-to-convergence cost term, no band numbers in the charter) and approved memory proposals A+C, applied with scoping clauses harmonized to #485's amended rule (the drafts predated the red-team and carried the exact wording it struck from D). Pre-images: `.pre-image/feedback_adversarial_review_catches_real_bugs.md.2026-08-28T114013.bak`, `.pre-image/MEMORY.md.2026-08-28T114013.bak`. Watch-item update: the red-team run REFUTED 8 findings, a live counter-datum to the 0-refutations concern. |

| 2026-08-28 | — (§7.3 verdict) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — | **Sammy: probation satisfied — team HIRED.** Rendered post-E2-S2-close. 10.2 proceeds through the team as standard practice; scheduled PM loop stays a future decision. Charter §7 updated same commit; memory index carrier corrected with pre-image snapshots. |

| 2026-08-28 | E2-S2 (reconciliation of the deployed row above) | — | — | *(bookkeeping — corrects an attribution, per the never-rewrite rule)* | — | The deployed row's verdict phrase "six rounds … drove ~36.5M" misattributes: ~36.7M is the STORY-cumulative verification across all four task-PRs (PR-trail cumulative 10.9M → 19.5M → 25.5M → 36.5M; ~17 rounds total), and #484's own six-round loop cost ≈ 11M (5 full + 1 scoped). Found by the band-recalibration red-team (59 agents, 2026-08-28) while re-deriving the §5 class table; the roadmap's E2-S2 header and §6.6 corrected in the same PR. The four token numbers in the deployed row are correct as written — only the rounds↔cost binding in the verdict prose was wrong. |
| 2026-08-28 | Band recalibration (Sammy-directed docs story) | — | — | *(bookkeeping — not a pipeline exit, does not count toward CURATE)* | — / ~16.4M (6 design red-team rounds, ~230 agents: 14+16+5+2+5+3 confirmed) / — / — | PR #487 opened for merge. Six rounds because four of five fix rounds introduced a new defect — the fix-round cascade the recalibration itself prices, occurring inside its own PR. Rounds 5-6 landed entirely on the tree-hygiene gate and the untracked handoff doc while the band model drew zero for three consecutive rounds, so the gate was SPLIT OUT to issue #486 with its evidence base (porcelain is path-level; `.claude/.gitignore` hides `worktrees/`; `--ignored=matching` cannot enumerate) and run-story §5 restored to main's #485 wording. One resume after a usage-limit kill (24 of 27 agents dead, all skeptics — findings would have read as unverified; resumed per the never-let-no-votes-pass rule). Also corrected: E2-S2's ~36.5M verify is story-cumulative over 4 PR loops (~17 rounds), not one 6-round loop — reconciled across 5 carriers; the untracked HANDOFF doc swept end-to-end (12 dead carriers, incl. two actionable merge instructions). |

| 2026-08-29 | E2-S2a | deployed | CAPTURE | Two earned amendments, no new files: D-T4 widens past `KILLS:` — all four defects across three rounds were false coverage claims in test *prose*, and the production DTO was correct at first commit and never touched by a fix; and `project_ggbc_dev_env`'s own backend recipe *caused* the 3× concurrent-postgres corruption because it hardcodes container/network names. 3 rounds / ~3.5M is on band at the floor on the recalibrated #487 model. | ~0.64M / ~3.50M (3 rounds, 16+18+12 agents; r1 2 distinct defects, r2 2 — **both introduced by r1's fix** — r3 zero-confirmed) / ~0 (code story) / ~0.09M | PRs: backend [#82](https://github.com/sammygallo/ggbc-backend/pull/82) `995e5a1a`, frontend [#490](https://github.com/sammygallo/goodgirlsbotclub/pull/490) `da37d8e6`. Cross-repo-contract escalation trigger fired under the newly-merged D2 (#488), presented at the gate; **Sammy said merge + deploy — both MERGED and DEPLOYED together 2026-08-29 21:53Z**, prod-verified (`/health` 200 on the 2nd poll, backend reached `Application startup complete.`). Deploy window enumerated per repo first: frontend 11 commits but only `da37d8e6` touches `src/`; backend 1 commit, **no migration** — the backend's deployed revision was derived from the ledger AND cross-checked against the droplet image's `Created` timestamp matching #81's build run, since ggbc-backend has no `.last-deployed` marker. Governance PR **#494 deliberately NOT merged** under the blanket "merge all ready" instruction — it touches `.claude/scripts/**` and `run-story/SKILL.md`, and I had never reviewed or presented it. **New defect found in `/deploy-ggbc` step 5.5:** its subscriber-notify loop greps `git log --merges` for "pull request #N", which cannot see the **squash** merges step 2 of the same skill mandates — so intake-linked features would silently never notify. Cost nothing here (no PR in the window was intake-linked). Release note skipped per the skill's own criteria: E2-S2a ships no UI. QA 4/4 AC, each mutation-verified. Pre-images: `.pre-image/MEMORY.md.2026-08-29T195106.bak`, `.pre-image/feedback_adversarial_review_catches_real_bugs.md.2026-08-29T195106.bak`. Proposals: P3+P5 applied directly (additive); **P1, P2, P4 APPROVED by Sammy and applied** (pre-images `.pre-image/{MEMORY.md,feedback_adversarial_review_catches_real_bugs.md,project_ggbc_dev_env.md}.2026-08-29T195748.bak`) — P1 is the big one: the dev-env memory's hardcoded `ggbc-test-pg`/`ggbc-test` names CAUSED the contention, so its recipe is now per-run (`$R`-suffixed) with `set -e` + `&&`, and it records that `pytest \| tail` returns tail's exit code and therefore fails GREEN. **P6 DECLINED by Sammy**; **`DECLINED.md` created** and seeded with it so it is not re-proposed. The postmortem also talked the PM out of its own `story-review` dedup-key idea — `repo\|file\|line` would trade a visible inflated count for a silently dropped defect — recorded as a negative result, not adopted. **Escalation, verified by me:** roadmap E5-S1 (unbuilt) cites `_activation.py:519-538` for sticky-carry-over behaviour — that range is RRF rank-fusion — and `:466-468` for the semantic/FTS legs — that is the timed-effects filter. Wrong subject, not a line shift — and the prose was stale too: E4-S0 (2026-08-26) fixed both halves the row describes as live defects, so its present tense described the pre-E4-S0 engine. **Fixed in [#493](https://github.com/sammygallo/goodgirlsbotclub/pull/493).** Two pre-existing defects filed at Sammy's approval: [#491](https://github.com/sammygallo/goodgirlsbotclub/issues/491) (`evictedEntryIds` has had zero frontend consumers since E4-S0) and [#492](https://github.com/sammygallo/goodgirlsbotclub/issues/492) (stale `129` golden count in 3 files, no test asserting the total — it propagated into a 4th place during this very run). Infra: concurrent pytest against one shared postgres corrupted results **three times this run** (story-dev, PM, then QA-vs-PM); each time it presented as ~368 failed / ~118 errors at *setup* in untouched files, and once masked its own exit code as 0. CI green on both heads (backend 10m34s, frontend 1m7s). CURATE counter: exit #3 since E9-S1. |

| 2026-08-29 | Pipeline hygiene (#486 + #489, Sammy-directed) | — | — | *(bookkeeping — not a pipeline exit, does not count toward CURATE)* | — / ~13.5M (4 design rounds, ~180 agents: 6+6+1+2 confirmed) / — / — | PR #494 merged on Sammy's go 2026-08-29 (governance surface — the trigger fired and routed it to him, as designed). Closes #486/#489. **The durable lesson is the shape of the fix, not the fix:** the tree-hygiene gate was written FOUR times as prose and shipped broken four times (noise-on-untracked · path-level so blind to content · a probe emitting a constant · per-tree so blind to sibling worktrees); it converged only once it became a script with tests. The lint written to catch the first failure reproduced the same cannot-fail defect twice more (guarded a header string not its bullets; then counted lines so blank-padding passed) before getting its own suite. Round 1 found a LIVE abandoned skeptic worktree holding an uncommitted mutation deleting the WI dedup guard — all three prose checks passed it; main unaffected, derelict removed. Round 2 found this session had itself deleted §6/§7/§8 (merge checklist + escalation-trigger header) via an index-arithmetic edit boundary, under a commit message about tree hygiene and a diffstat reading '5 insertions, 29 deletions'. Controls now state their threat model honestly: they detect ACCIDENTS, are not integrity boundaries, and a same-length reword passes. |

**CURATE counter starts with the E9-S1 row above** — it is exit #1. **Four** pipeline exits predate the role and do not count toward it, not two: E1-S1 and E2-S1 (the rows above), plus **E4-S0 and E9-S6, which completed full runs on 2026-08-25/26 — hours before this ledger merged at 22:58Z — and so were never rowed at all.** E9-S6's absence is the one that cost something: it carries the first recorded build leg (1.04M), and because no row records it, `MEMORY.md` went on asserting the dev leg had never run, which is the stale premise E9-S1's own run record then repeated back to the postmortem.

**The `tokens` column holds estimates, not measurements.** There is no per-stage meter; the PM reports its own best estimate. Reason #3 above — accumulating these until a postmortem band can be set — is weaker than it reads. Say "estimated" when you set one.

| 2026-08-30 | E2-S5 | blocked | CAPTURE | **A comment that was TRUE when written is a defect surface no re-read catches.** E2-S5's premise was accurate on 2026-08-06 (`9ced3e43`), falsified the next day by `77e689d2`, and quoted verbatim as a card premise 24 days later; INTAKE's ground-truth rule killed it for ~0.35M before any build leg — but the correction then reproduced the class twice, because a claim about WHEN or BY WHICH COMMIT is not verifiable by re-reading the file (a dated attribution wrong in 8 places passed four of six rounds and was caught by `qa-verifier`, not a fifth round) and because prose the run emits outside the diff — filed issue #495, whose fourth carrier QA also refuted — is read by no review round at all. | ~0.35M / ~1.15M (6 rounds; QA measured at 241,512 + 297,781 = ~0.54M of it) / ~0.35M / ~0.14M | **Blocked at INTAKE: premise falsified, not shipped.** E2-S5's AC1 (the crosswalk) is a no-op since `77e689d2` made local entry ids identical to the server's; AC2/AC3 survive re-framed, so **Sammy chose re-scope-in-place over closing the card** and E5-S1's gate stands. Deliverable = [PR #496](https://github.com/sammygallo/goodgirlsbotclub/pull/496), **zero behavior change**: origin comment corrected in-tree, 8 roadmap carriers fixed, §1 correction row added, the dated 2026-08-26 intake resolution ANNOTATED not rewritten, board refreshed + republished, memory `project_token_breakdown_analysis` amended. **Do NOT model blocked ⇒ zeros** (postmortem escalation 1): E9-S1's row reads `0/0/0/~1.56M`, but here the correction *was* the deliverable, so verification and absorption are both real. **Review: 6 rounds, 5 confirmed defects, all in the correction's own prose** — R1 named an impossible drop case (shared-book, which makes a chat *ineligible* for server retrieval — backwards) and overstated two tests' coverage; R2's defect was introduced by R1's fix (third consecutive story with a fix-round-introduced defect); R3 a frequency-vs-possibility ambiguity; R4 clean; **R5 found by `qa-verifier` after four of my rounds missed it** — the cutover was misattributed to #71/#435/#74 / 2026-08-23 with "17 days later", wrong in 8 places, when `docs/legacy-id-strip-scoping.md:27` already named `77e689d2` "the 2026-08-07 cutover commit"; R6 confirmation clean on all 6 points. **QA also refuted one of my own issue #495's four carriers** (`worldInfoStore.ts:1556` is not one — `GET /lorebooks/{id}` sorts by `insertion_order` alone with no `id` tiebreak, `lorebooks.py:155`, so its argument survives id convergence); verified against the backend, #495/§7/board corrected to three. Filed [#495](https://github.com/sammygallo/goodgirlsbotclub/issues/495) (E4-S0 sticky tie-break cites a dead justification; two of three carriers are sha256-guarded byte-identical across both repos, so they cannot move independently — not a live defect). Gates: eslint 0 errors, vitest 102 files / **1978** tests, build exit 0, tree-hygiene CLEAN. **Tree-hygiene's first live catch since #494 merged: a derelict SIBLING worktree** (`.claude/worktrees/agitated-germain-fef44e`) with uncommitted vite dep-cache churn — HEAD already an ancestor of `main`, nothing stranded, restored the one file rather than removing the worktree. That is exactly the class all four prose versions were blind to. My own error: I snapshotted before editing rather than before the review stage, so the gate correctly fired on my intended changes — sequencing mistake, not a gate defect, no doc change earned. Pre-images: `.pre-image/project_token_breakdown_analysis.md.2026-08-30T160911.bak` (PM-stage INTAKE amendment — the E9-S1 fix firing for real), `.pre-image/{feedback_adversarial_review_catches_real_bugs,MEMORY.md}.2026-08-30T162212.bak`. Proposals: **P1 (D-T5 taxonomy bullet) + P2 (index line) applied directly** (both `removes_content: false`); **P3 awaits Sammy** — a `removes_content: true` doc-edit broadening `run-story` §1's carrier-sweep clause to fire on "architecture moved under it", not just "already shipped", and to name **a code comment as a carrier**; it is a governance surface so it needs its own PR + design-mode red-team. Postmortem **declined to manufacture an INTAKE change** (the ground-truth rule worked — second blocked-at-intake kill by the same rule) and declined a reviewer-stance edit on charter §4.6 thin-agent grounds. New band datum, recorded not acted on: 6 rounds over a prose-only zero-behavior diff ≈ **~0.19M/round**, below §5's cheapest named unit — the units were derived from code diffs and subject size is a term the model does not name. Carried forward unresolved from E2-S2a: the `/deploy-ggbc` step-5.5 squash-blind subscriber-notify defect, still no issue. **CURATE counter: exit #4** since E9-S1 — threshold ≥5, so **CURATE fires on the next exit**; no CURATE row has ever been written. |

| 2026-08-30 | E2-S5 follow-up (to the row above) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — / ~0.15M design red-team of P3 / — / — | Sammy: **"apply the postmortem findings and merge everything else."** All three PRs merged. **[#496](https://github.com/sammygallo/goodgirlsbotclub/pull/496)** `1244da60` — the E2-S5 premise correction itself. **[#497](https://github.com/sammygallo/goodgirlsbotclub/pull/497)** `47bc734e` — **`/deploy-ggbc` step 5.5's squash-blind notify loop, carried forward unfixed since the E2-S2a row, now fixed as a tested script.** The defect was worse than that row recorded: measured over a real 12-commit window with six merged PRs (`fba4c20b..1244da60`), the old `git log --merges \| grep "pull request #"` returns **nothing** while the new script returns `487 488 490 493 494 496`, matching ground truth — so **no intake subscriber has ever been notified that their request shipped**, and it was invisible because an empty PR list is indistinguishable from the common "no intake-linked features shipped" case. Built per #494's precedent (11 tests; **both mutants killed** — restoring the `--merges` pipeline fails 3 cases including the squash case, removing the `(#N)$` anchor fails the false-positive case, which matters because the list feeds real DMs and an unanchored match would message PR 99's requester over `fix: handle (#99) case in the parser`). Extraction also moved off the droplet so it is testable at all, and an unknown sha now fails loudly rather than returning empty. Left as-is and documented: the marker advances before any DM is sent, so a mid-loop failure loses notifications; the alternative trades that for duplicate DMs — a product call. **[#498](https://github.com/sammygallo/goodgirlsbotclub/pull/498)** `04dbe661` — postmortem **P3 applied** (`removes_content: true`, gated, approved): `run-story` §1's carrier sweep now fires when **"the architecture moved under it"**, not only when the story already shipped, and names **a code comment as a carrier**. **P3 was red-teamed before applying and one finding was fixed rather than shipped:** the proposal added "and the source tree" to a sentence already ending "sweep the *source* file end-to-end", where *source* meant the file that carried the claim — together they read as "source-code file", so that phrase is now "the file that carried the claim". Five other lenses cleared (over-firing, unbounded sweep, `preserved[]`, loophole in the new exit, cross-stage contradiction); all 11 original clauses verified still present; `skill-lint` INTACT. **That makes four instances in one day of a correction carrying the defect class it was correcting** — the comment, my two review rounds, my own issue #495, and now the postmortem's own proposal. **Note for whoever reads this next: `~/.claude/skills/*/SKILL.md` are symlinks to the WORKING TREE**, so both `/deploy-ggbc` and `/run-story` changed for every session the moment the file was written — merge and deploy are irrelevant to that, which makes `.claude/**` sharper than §8's wording implies. CI green on all three heads (1m6s / 1m9s / 1m7s). Nothing deployed this session — `main` now holds three merged commits awaiting the next deploy anyone approves, none with runtime behavior. **CURATE still due on the next pipeline exit** (E2-S5 was exit #4; this row is bookkeeping and does not advance the counter). |

| 2026-09-01 | E2-S5 (re-scoped run, PR #500 — rowed retroactively) | deployed | — | *(no verdict: the postmortem did not run for this exit, which by rule 2 above is a skip, not a clean run. The session that built, reviewed and merged the run appended no row; E9-S7's PM appended this one on 2026-09-01 after E9-S7's review found the gap.)* | ~1.2M / ~12M (6 rounds; ~160 agents per the roadmap §7 cell) / ~0 (code story) / — | The continuation of the 2026-08-30 blocked-at-intake exit above, after Sammy chose re-scope-in-place: read-time remap of pre-cutover `wi_fired` keys (`remapWiFiredKeys`) plus `notObservable[]` in `wiReplay`; QA 4/4 AC, every guard mutation-verified (roadmap §7 cell). Merged 2026-08-31 as `2b3efa30` ([#500](https://github.com/sammygallo/goodgirlsbotclub/pull/500)) at the escalation gate on Sammy's explicit go with merge-checklist item 3 knowingly waived — the "review did not converge cleanly" trigger had fired. Deployed 2026-09-01 by a sync `/deploy-ggbc` run (frontend image `9474fbfa`, `/health` 200 on the first poll); §7 moved the card to Deployed in `f70f281b`. Build and verification figures are from the board focus panel as of `1c181557` (since overwritten by E9-S7's panel), not measured here; PR #500's body, whose review table predates the round-6 comment on the PR, tabulates 5 rounds, 127 agents and ~9.8M, and the round-6 comment adds 33 agents. **CURATE counter: exit #5 since E9-S1 — the ≥5 threshold was met at this exit, and CURATE did not run because no postmortem ran; CURATE is due at the next pipeline exit.** Carried forward unresolved: this run's CAPTURE postmortem, still not run. |
| 2026-09-02 | E9-S7 | deployed | CURATE | **The fix-round rule has no SUBJECT bound, and the PM's own bookkeeping rides the story branch into it:** 19 rounds / ~19M on an S story whose §5 class budget is one ≤0.3M standard pass (~63×), with rounds 4–19 touching no code at all — ~15M reviewing the board and ledger prose the PM wrote, text that describes the review still running, so every round falsified what it had just reviewed, ending in seven rounds that reversed each other on one idiom. Build was ON band (~95k initial, ~125k rework), which per §6.5 isolates this as pure verification scoping. Two mechanical multipliers, one clause each: `/code-review` silently reused a previously-typed xhigh, making every "standard pass" a ~1M instrument; and PR #504 carries `ggbc-board.html` and `docs/agent-team-log.md` in the same diff as `chatStore.ts`, so the full-lens fix-round rule pulled the PM's self-describing prose into every round. The loop's one real catch — E2-S5's deployed exit (#500) had no ledger row, so the CURATE threshold was met at that exit and CURATE silently did not fire — is exactly the class that belongs in a deterministic check at step 10c rather than luck in a ninth prose round. | ~0.22M / ~19.5M est. (19 rounds — 1 code round with 3 confirmed defects, 2 rounds of comment/test hygiene, 16 rounds over the PM's own board and ledger prose; incl. QA ~0.09M) / ~0 (code story) / ~21M est. (CAPTURE+CURATE workflow 15.6M by the completed run's counter, 347 agents, after a usage-limit kill of its first attempt at 160/347 whose counter read 11.2M with unknown cache overlap; design red-team of PR #505 5.5M by its completed run's counter after a 3.0M killed attempt, plus a 13-verifier resume in flight at row time) — all figures estimates | Shipped: [#504](https://github.com/sammygallo/goodgirlsbotclub/pull/504) → `473c8aa2`, merged on Sammy's explicit go after the "review did not converge cleanly" trigger fired at the gate; deployed 2026-09-02 02:5xZ (frontend image `3aea98b8`, `/health` 200 on the fourth poll); release note posted; #455 closed; #501, #502, #503 filed. QA 4/4 AC, latch-reset kill re-run red in a throwaway worktree. **Corrections to this run's own record:** the PR body's "CI event silently did not fire" was an overclaim — the PM re-triggered after 82 s (killed 3/3 by the postmortem's skeptics, corrected in the GitHub-Actions memory); the PM replaced a `MEMORY.md` index line at INTAKE without a pre-image (second occurrence of the E9-S1 class; the replacement's own framing was also wrong and is now corrected additively); and the retroactive E2-S5 row above was written on the story branch and re-reviewed for nine rounds, which the postmortem's rule 1 now forbids. **CURATE (first ever): sweep 53/56 memory files, partial — `feedback_chrome_caches_301s`, `feedback_workflow_empty_result_check_failures`, `project_extensions_pickup` were not swept (critic spot-checked all three: not stale); 1,756 claims checked, 261 stale claims verified by refute-first skeptics (201 additive, 60 removing/rewriting), 3 refuted.** The completeness critic judged the set unsafe to apply mechanically (mass-additive notes on dead files are accretion; competing fix_texts for one index line; self-falsifying texts) — so **none of the 261 was applied**; the whole set is handed to Sammy as a batch decision (`curate-gated.md` + `curate-additive.json` in this run's scratchpad, summarised in the close report), with the critic's recommendation to rewrite or delete `project_phase8_progress`, `project_phase9_progress`, `project_phase87_pickup`, `reference_story_state_schema_v1`, `project_backend_database`, `project_multiuser_system` instead of amending them. Proposals: **P1–P4 (doc-edits to `run-story`) → PR [#505](https://github.com/sammygallo/goodgirlsbotclub/pull/505)**, design-red-teamed (41 attacks, 23 upheld, 18 refuted) and rewritten; **P5 killed 3/3**; **P6 (row 18 in `feedback_adversarial_review_catches_real_bugs`) and P7 (index clause, minus a false commit-range claim the critic caught) applied directly**; **P8 (`removes_content: true`, four words) awaits Sammy**; two further additive corrections applied by the PM (the line-45 correction; the CI re-trigger note). Pre-images: `.pre-image/<every memory file>.2026-09-02T101551.bak` (57 files). DECLINED.md: one entry, not re-raised. Escalations: roadmap §5 needs a SUBJECT term or the P1–P3 rules (Sammy's call); reviewer-stance pair divergence (`adversarial-reviewer.md` carries a safety-gate suspicion bullet `story-review.js`'s constant lacks) — governance, own PR if Sammy wants it; QA's server-permission residual REFUTED by the PM (the settings gear is gated by the legacy `can()` shim, which never reads `user.permissions`); the `/deploy-ggbc` step-5.5 carried-forward item is closed (#497). Infrastructure waste: two usage-limit kills (session limit; Fable model limit), ~14M of counters on killed attempts with unknown cache overlap; resume worked both times. **CURATE counter resets here (exit #6; first CURATE row).** |
| 2026-09-02 | E9-S7 follow-up (to the row above) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — / — / — / ~1.9M (the design red-team's final 13-verifier resume; its two earlier attempts are in the row above) | Sammy: **"merge #505"** — the four `run-story` amendments merged as `f4b7eda2` on his explicit go (governance surface; CI green on head `c7c5bafe`). Final shape after the design red-team completed 90/90 (25 upheld, 16 refuted; synthesis: nothing blocks on text grounds, residual fixes landed): §5 the review subject is the deliverable diff and bookkeeping gets a pasted read-off, never a round; §5 a prose finding must name the check that fails, closed REFUTED-with-reason, with §8 item 2 admitting that closure; §5 Standard tier reads both the echoed level and target and re-invokes until right; §10c reconciles real exits (`gh` first, then §7 Deployed and the ledger) before the CURATE count, and a retroactive row says `no row found at reconciliation on <date> by <run>`, never `no postmortem ran`. The MERGE board refresh moves to step 7 before ready. Known limits recorded in the PR body: the bookkeeping check is a self-check by its author; rule 4 rests on PM judgment and on Sammy's answer about live sessions. Still awaiting Sammy: P8 (four-word memory rewrite), the 261-claim CURATE batch, the roadmap §5 SUBJECT term, the reviewer-stance drift. |
| 2026-09-03 | E9-S9 | parked | CAPTURE | **E9-S9 spent ~24.7M over six trigger-tier review rounds — 7–14x its "trigger-tier, contained seam" class budget of ~1.7–3.5M — and shipped nothing, because the two §8 escalation triggers that its own review loop satisfied are read only at MERGE, which a non-converging loop never reaches: "scope grew beyond the AC" was true from round 1 (F3's fix added a module-level identity memo for a deleted-while-open case the AC never named and the plan never probed) and "two or more fix rounds each introduced a new defect" was true from round 3 and then repeated through round 6, five consecutive rounds all in one predicate inside that widening. P3's "review every fix round with full lenses" was followed and was correct; what was missing was a STOPPING rule, so the durable fix is to read §8's triggers at the top of every fix round rather than only at merge. The run's real outputs were ggbc-backend#84 and the finding that a group chat's identity rests on three UI-owned inputs — a registry record deletable while its chat is open, a global mode flag whose ChatView ref never records true, and the memo the fix itself introduced — each of which is wrong in some reachable state on main today.** *(PM note: the run record never named the §5 class row; the story's surface also fits* backend contract *(~3.2–9.5M), so the multiple is ~2.6–14× depending on class — and the board card at PLAN declared* standard review at high*, i.e. a ≤0.3M pass; §6.1's* async store orchestration *trigger selected the trigger tier at REVIEW and the card was corrected only at park.)* | ~0.7M (initial ~0.1M + ~0.6M rework across five fix rounds) / ~24.7M (6 trigger-tier `story-review` rounds — 3.56 / 3.01 / 3.77 / 2.83 killed by the session usage limit at 24 of 61 agents + 2.72 resume / 3.58 / 5.24M; 37/33/43/61/41/61 agents; 10/13/17/20/9/22 confirmed; rounds 1–5 yielded 5/6/6/6/4 distinct items, round 6's 22 were never worked) / ~0 (parked before QA) / ~2.14M (23 agents: CAPTURE ~0.25M + 3 refute-first skeptics × 7 proposals + completeness critic) — PLAN ~0.83M (judge panel, 3 designs × 3 judges; frozen identity won 106–91–54) is outside the b/v/a/p columns | **Sammy: "park it"** at review round 6, after the PM stopped the loop and presented descope / keep-hardening / park. Branch `claude/e9-s9-group-recall-identity` @ `f892d731` pushed, **no PR**; the frozen-`identityAvatar` core (record field + slot-0 backfill, read by save/load/recall/beacon) is sound and covered — 106 files / 2044 tests green, 6 kill mutations red at build, eslint 0, build 0 — and the descope-and-ship option remains open when [ggbc-backend#84](https://github.com/sammygallo/ggbc-backend/issues/84) (server-side group identity / re-key) lands. What did not converge: keeping a deleted-while-open group addressable — round 1's F3 (sidebar delete of the OPEN group) was absorbed as a fix (module-level identity memo) instead of FILED, and rounds 2–6 each caught a defect the previous fix had introduced in `persistTruncatingEdit`'s group-vs-solo predicate and that memo (fix-cascade class, cf. E2-S2 3/6 and E9-S7); round 6 traced the recurring state to pre-existing UI bugs on `main`, now filed as [#506](https://github.com/sammygallo/goodgirlsbotclub/issues/506) (`ChatView`'s clear-chat effect never records group mode → re-selecting the pre-group character leaves the group transcript open in solo mode and the next send forks the row) and [#507](https://github.com/sammygallo/goodgirlsbotclub/issues/507) (`deleteGroupChat` leaves the open chat with no record). Round 3's boot-time re-upload of repaired identities (`b49d6f10`) was removed in round 4 (`b6fc8eeb`) as unsound (published slot-0 guesses, made a read-only boot a writer) and round 5 showed removing it changes nothing — every ordinary save PUTs the whole `stm_chat_state` blob — which is the no-client-fix finding behind #84. Roadmap §7: E9-S9 → *Backlog — gated ← ggbc-backend#84*; §5 row records the park; board republished; #458 commented (round table = this row) and its dead `chatStore.ts:895-903` pointer replaced with symbol cites here, in roadmap §5 and in `docs/prompt-injection-audit.md` rows 18/21/23 (D-T5 instance: correct at `cb9bb2e7`, drifted 56 lines in eight days with the block untouched). **Hygiene:** round-4 skeptics left two derelict throwaway worktrees — the third recorded recurrence (E1-S1, E2-S2 rows) of a rule present and in sync in both stance copies — and `tree-hygiene.sh` caught it, its first derelict-worktree catch; no prose proposal (the gate, not the instruction, is the working control). **E9-S7's rules held on their first run:** no bookkeeping entered a review round; the trigger tier was selected by §6.1 and the workflow ran on the branch diff. Postmortem: 7 proposals, 21/21 skeptic votes upheld-with-amendment, critic found 9 sequencing/provenance gaps (applied: ledger row first, issues filed, class stated beside the declared tier, P1 corrected). Applied by the PM: **P3** new defect class D-C9 (a new field on a record inside a whole-section synced blob is stripped by an older client's key-list normalizer and re-derived wrong) and **P4** D-T5 extension (`file:line` cites rot with a one-commit half-life; cite the symbol), both additive to `feedback_adversarial_review_catches_real_bugs.md` (pre-image `.pre-image/…2026-09-03T135102.bak`); **P7** the pointer fixes in this commit. **P1** (a §5 rule: read §8's triggers at the top of every fix round — stop-and-present on scope-adding fix or two consecutive fix-introduced defects in one seam) → draft governance PR [#508](https://github.com/sammygallo/goodgirlsbotclub/pull/508), design red-team pending, Sammy merges. **Gated for Sammy** (`scratchpad/e9s9-gated.md`): P2 (the E9-S9 row for that file's §4 table — held only because the E9-S7 CURATE batch proposes rows on the same table), P5 memory-new `reference_group_chat_identity.md` (why no client-only fix works before #84), P6 MEMORY.md index rewrite (removes the stale run count — a SECOND text for a finding the withheld CURATE batch already carries; decide together). Negative results recorded: PLAN-stage input-enumeration rule rejected (the defective predicate did not exist at PLAN); no §5 calibration-table entry (pathological datum). CURATE not due (exit #1 since the E9-S7 CURATE row). Still with Sammy from E9-S7: P8 4-word rewrite, the 261-claim CURATE batch, roadmap §5 SUBJECT term, reviewer-stance drift PR. |
| 2026-09-03 | E9-S9 follow-up (to the row above) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — / — / — / ~5.8M (design red-team of PR #508: 5 lenses × `adversarial-reviewer`, 31 findings × 2 refute-first verifiers, 68 agents — ~2.08M killed by the session usage limit at 20/68 + ~3.69M resume from cache) | Sammy: **"merge #508"** — the E9-S9 postmortem's P1 merged as `b773258b` (squash, on his explicit go; governance surface; CI green on head `e4988ced`; branch deleted). Final shape after the red-team (24 upheld / 4 split / 3 refuted → ~10 root defects, all resolved on-branch in `67a595e8` + `e4988ced`): **§5** a bullet read at the top of every fix round — Q1 *does this fix add a mechanism the AC never asked for, for an exposure the story's own change created* (classify the exposure first: pre-existing-and-adjacent → FILED-as-issue and continue; story-created but fail-closed-able → fix, no escalation; story-created and the fix is a new mechanism → stop and present), Q2 *has §8's did-not-converge trigger already read true* — both disjuncts (two or more fix rounds each introduced a new defect, OR the round count ran past the roadmap §5 class row), unnarrowed, class row by membership test not the card's declared tier; either yes → stop and present §8's summary plus which question fired, spend vs the class row (reported, not blocking) and the decidable choice; a keep-going authorizes the review only and the trigger is re-read at merge; per-round answers recorded at §7 in the evidence bundle (`round N: Q1 no / Q2 no`), a missing line is a skipped round; **§5 / §8 item 2 / charter §4** FILED-as-issue as a bounded fourth finding closure (pre-existing-and-adjacent only; issue link + reproducing `main` sha). *Correction to the row above:* its one-line summary of P1 ("two consecutive fix-introduced defects in one seam") describes the draft; the merged Q2 is §8's test, both halves — consecutive-same-seam survives only as the diagnosis to name. Two blockers the red-team found in the draft: Q2 claimed §8's wording while being strictly narrower (E9-S7's shape — round count past band, no fix-defect pair — would have answered no every round), and "FILE it" was a closure no governance doc admitted (a REAL finding on `main` could only be ACCEPTED, which itself fires a merge trigger) — the second is why the PR widened to the charter. Declined with reason: a ≤250-word gate form; editing roadmap line 218's "(§6.1)" self-tag here (protocol section → Sammy); a mechanical "seam" definition (would exclude E9-S9's memo, which did not exist at BRIEF). |
| 2026-09-03 | E9-S9 (gate-check run — second run of this story) | blocked | CAPTURE | **Blocking at INTAKE on a gated card worked as designed at ~0.2M and needs no new rule** — but the ground-truth pass exposed two bookkeeping-durability defects: the E9-S9 park refresh (`96249fc3`) added a Gated card without removing the story's stale `ungated` Ready-for-dev card, count or rail tile, so main carries two contradictory E9-S9 cards; and every decision still owed by Sammy from E9-S7 and E9-S9 sits in one past session's `/private/tmp` scratchpad. *(PM note, from the refute-first panel and the two critics that followed: the first clause is confirmed and UNDERSTATED — `96249fc3` left three board defects, not one, and the Gated count was already wrong before it. The second clause was refuted 3/3 and is NOT a defect: ledger rows 45 and 47 already index every pending item by identity, which is exactly how this run enumerated them, and the expiry mechanism the proposal blamed does not exist on this machine — `/private/tmp` is not age-cleaned here; the real exposure is reboot, and the files were rescued this run. All three proposals were refuted as forks of §5's read-off bullet, so the honest reading is non-compliance with a ~14-hour-old rule, not a coverage gap.)* | ~0 / ~0 / ~0 / ~1.24M (**measured**, not estimated — the postmortem ran as a workflow: 13 agents, 186 tool calls, 0 errors; record-verification pass + CAPTURE + 3 refute-first lenses × 3 proposals + 2 completeness critics) — plus INTAKE ground-truthing ~0.2M est. (roadmap §5 pipeline overhead, band ~0.05–0.8M) | **Sammy: "A"** — wait for [ggbc-backend#84](https://github.com/sammygallo/ggbc-backend/issues/84). The PM presented A wait / B re-scope and ship the frozen-identity core fail-closed / C build the rekey endpoint first, and recommended B; Sammy chose A. **Blocked at INTAKE on one Definition-of-Ready line** (dependencies verified against ground truth). The premise is INTACT, not falsified — unlike E9-S1 and E2-S5 — so no carrier sweep was owed. **Gate ground truth:** #84 OPEN, created 2026-09-03T15:45:47Z, zero comments, no assignee/label/milestone, no backend branch or PR; its timeline holds only the three same-day cross-references from #458/#506/#507. Branch `claude/e9-s9-group-recall-identity` @ `f892d731` on origin, no PR, not an ancestor of `main`; the six `src/stores/` files merge clean, and the two conflicts are `ggbc-board.html` (against the park's own `96249fc3`) and `docs/prompt-injection-audit.md` (against `542eeac0`). #458/#506/#507 all open. WIP zero. **Ledger reconciliation** over the window since the E9-S7 CURATE row: #504/#505/#508 frontend, none backend, each already rowed — nothing owed. **CURATE not due** (exit #2 since the E9-S7 CURATE row). **Board corrected in this run's commit** — the one thing actually owed, under §3 line 50 and charter D4 (§7 wins; fix the board), not under any proposal: `96249fc3` bumped Gated 18→19 and added the parked card but never removed the story's stale `ungated` Ready-for-dev card, its `ct` or its rail tile, so `main` advertised the PARKED story as the single runnable item. Fixed all four in one commit rather than splitting: stale card removed, Ready-for-dev `ct`+tile 1→0, Gated `ct`+tile 19→17, `.stamp` sha `37447003`→`f4363368` (the park updated the stamp's date and left its sha). **The Gated drift was pre-existing** — `ct` exceeded cards by exactly 2 at both `96249fc3~1` (18 vs 16) and `96249fc3` (19 vs 17), so the park's increment was correct on a wrong base. All eight columns now reconcile card-for-card against §7 (§7's gated row has 17 entries; the naive id grep returns 26 because it also catches gate dependencies — count leading ids of the ` · ` items). **The `ct` semantic is undocumented but not undefined:** ct = cards in column = §7 entries in the matching row, obeyed by 7 of 8 columns before this fix. **NOT fixed, needs Sammy — pre-existing drift in the board's "Filed, no story card" section:** its tile and prose both say eleven, it lists 8 issues (#454 #457 #466 #468 #476 #477 #478 #495) and §7's row lists 9 (#454 #457 #466 #467 #468 #469 #470 #471 #495); board-only #476/#477/#478, §7-only #467/#469/#470/#471, and neither carries #506/#507 filed by the parked run. Reconciling it means deciding which set is right, which is a planning call, not a rendering correction — and §3 lets a run touch only the counts its own card moves, so whole-column drift has no owner. **Pending decisions rescued to a durable path** (a copy; originals untouched): `curate-additive.json` (201), `curate-gated.json` (60), `curate-gated.md`, `e9s9-gated.md` → `<memory-dir>/.pending/` with a README recording provenance. 201+60 = row 45's 261 claims. Verified exposure: `/private/tmp` is NOT age-cleaned on this machine, but uptime was 21 days and the oldest surviving entries are stamped minutes after `kern.boottime`, so the queue would have gone at the next reboot. **No memory entry written, no pre-image taken** — zero proposals survived, so nothing was applied and nothing was gated. Memory budget unchanged (55 entries, MEMORY.md 18,386 bytes). **Confirmed working:** INTAKE's ground-truth line; the re-scope clause generalizing from a partly-dead AC list to a gated-but-intact card; §10c's reconciliation backstop; the WIP check. **Not exercised:** #508's Q1/Q2 fix-round questions — no fix round ran, so this run is evidence neither way. **Open for Sammy** (raised, deliberately not decided): (1) do sub-0.5M refuse-to-start exits tick the CURATE counter? Three more gate-checks on an untouched #84 would fire a full sweep off non-runs, and row 45 shows a sweep costs ~21M. The ledger's "(bookkeeping … does not count toward CURATE)" marker used in four rows is documented in neither the header's Columns spec nor §10c. (2) #84 is unowned and unscheduled and absent from §7's "Filed issues with no story card" row, so this ~0.2M gate-check recurs on every re-invocation with no path to clearing. (3) whether the board caused the invocation — `main`'s board named the parked story as the single `ungated` Ready-for-dev card; the PM did not select it from the board (the story id was the command argument), but whether Sammy did is not something this run can establish. (4) this postmortem cost ~6x the run it examined. **Still with Sammy:** E9-S9's P2/P5/P6 and E9-S7's four items — now readable at `<memory-dir>/.pending/`. |
| 2026-09-03 | E9-S9 gate-check (follow-up to the row above) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — / — / — / — | **Two corrections to the row above, appended rather than edited (ledger rule: never rewrite a past row).** **(1) "WIP zero" is misleading.** It was read from `gh pr list` and no open team PR existed, which is §1's literal test — but **E8-S1 is in flight**: local branch `claude/e8-s1-arch-v2-methodology` in its own worktree `.claude/worktrees/e8-s1` @ `6c48ba40`, unpushed, so no `gh` query can see it. Actual WIP at this exit was 1 of 2, which would not have blocked E9-S9; the defect is the claim, not the decision. A design story that never opens a PR until its doc is approved is invisible to the WIP check as written. **(2) The published board artifact was AHEAD of the committed source**, discovered at the §3 read-before-publish step *after* the row above was written. The E8-S1 session had published its in-progress state — `active E8-S1` in the stamp, its card `s-crit`→`s-live` with a "Running 3 Sep" line and branch, footer date 2→3 Sep — to the artifact URL without committing any of it to `.claude/skills/run-story/board/ggbc-board.html`. It had also already removed the stale Ready-for-dev E9-S9 card and set that count and the stamp sha, so the row above's account of who fixed what on the LIVE board is wrong: on the artifact those were already fixed, and only the Gated `ct`+tile 19→17 was this run's. The committed source did carry every one of those defects, so the source-side description stands. **Nothing of theirs was lost** — reconciled per §3 (port the other session's card forward, publish the source), committed as `b093f97d`, republished to the stored URL; the source is now the live version plus the Gated fix alone. **Standing risk this surfaces:** the source of truth for the board is the committed file, but the artifact can be advanced independently, so a session that publishes a working copy without reading first silently reverts another run's card — the near-miss §3 already records (#489), reached here from the opposite direction (live ahead of source rather than behind). |

| 2026-09-04 | E8-S1 | parked | CAPTURE | **A trigger-tier review's cost is knowable before its skeptic wave launches, and §8's convergence trigger cannot see it.** E8-S1's two design passes cost ~18.3M live (~20.7M with the usage-limit kill; 2.1–6.1× the claim-set row) while the round count stayed inside the row's expected 1–2 passes the entire time — so question 2's round-count half correctly read *no* at every read and the budget still went ~2× over on round 1 alone. `story-review.js` fans out `lenses + 2 × deduped findings` agents (exactly 145 and 89 here) and logs the dedup count at the barrier *before* the skeptic wave — 140 of round 1's 145 agents — so ~10M was predictable ~18M earlier than it was learned; six recorded passes sit at ~70–90k per agent, and roadmap §5's flat ~3.4–4.9M claim-set unit silently assumed ~17–32 findings against this doc's 70. Two governing-document contradictions were also measured, not merely read: the charter's design-story variant vs roadmap §5's L-minimum exemption for E8-S1/E3-S1/E6-S1 (the exemption is false, and cost ~18.3M of unbanded verification against an M build band), and the variant's own mechanics, where the charter and the skill are each right about one half and wrong about the other. And #509 is broader than filed — `google.py` hoists system-role messages exactly as `anthropic.py` does, so depth injection is inert on 2 of the backend's 13 catalog providers, not 1. | ~0.9M (4 research passes 569k + drafting + 1 fix round) / **~20.7M, 2 full design passes** (round 1 attempt 1 ~2.34M killed by the Fable usage limit at 127 of 145 agents and discarded UNREAD per charter §2, then ~10.30M on resume with 145/145 and 0 errors; round 2 over the fix delta ~8.04M, 89 agents) / ~0.22M (OpenRouter follow-up research 0.12M + carrier corrections) / ~0.17M | **Parked by Sammy** ("park it and pick it up when E8-S2 is scheduled") at the `run-story` §5 early-trigger stop, read at the top of what would have been fix round 2: **Q1 no** (all remaining findings were corrections to the deliverable's own gate criteria; the one product exposure was pre-existing and FILED), **Q2 yes on the round-count half** (claim-set class row = 1–2 passes, two had run, a third was required). The three-way choice — descope the gate layer and ship / keep hardening / park — was presented and Sammy chose park. Branch `claude/e8-s1-arch-v2-methodology` @ `fbb30fdf` pushed, **no PR**; worktree removed, tree-hygiene CLEAN. Deliverable: `docs/character-architecture-v2-methodology.md` + four research reports under `docs/research/e8-s1-r*.md`. **Converged:** methodology pick (behavioral definition compiled into existing card fields, zero builder change), three-arm design with a token-matched freeform control, fully scripted probes, lore off, the character-attributable token surface. **Did not converge:** the statistical gate layer — threshold scale after the ties-at-0.5 change, the verdict-composition rule over 2 orders × 2 judges, non-inferiority margins, depth-cell parity. Review: round 1 72 raw / 70 deduped → 23 confirmed · 5 plausible · 42 refuted · 0 unverified, all 28 fixed in `fbb30fdf`; round 2 (full lenses over the fix delta) 42 → 15 confirmed · 11 plausible · 16 refuted · 0 unverified, **round 1's fix having introduced several of them, concentrated in that one layer**. R3's research overturned the story's own hypothesis pre-code (arXiv:2601.04716 found structured-vs-unstructured profiles negligible), which is why the brief's claim narrowed to behavioral *content* at parity plus a control arm. **Filed [#509](https://github.com/sammygallo/goodgirlsbotclub/issues/509)** (P10, pre-existing-and-adjacent, reproduces on `main` at `f4363368`): the Anthropic and Google translators hoist every system-role message to a single top-of-prompt field, so the character's note never injects at depth and post-history instructions are not last on 2 of 13 catalog providers; the OpenRouter path bypasses it (`family="openai"`), and Anthropic's API has since added mid-conversation system messages on newer models, so the fix is cheaper than first filed — both recorded as issue comments. Bookkeeping commit `c966d8e6` also fixed the drifted `worldInfoStore.ts:1250` pointer across three carriers (symbol cite: `applyTokenBudget`'s cost lambda). Board reconciled cross-session **in both directions**: another session's Gated 19→17 correction was verified and ported into the source (`f8695bd6`), and main's copy had already been reconciled forward from this run's publish. Artifact watch fired three stale notifications for an already-reconciled version and was unwatched. Pre-images: `.pre-image/{feedback_adversarial_review_catches_real_bugs,reference_rag_lorebook_injection,MEMORY}.md.2026-09-04T132940.bak`. **Proposals: P5 (new defect class D-T6), P6 (provider-conditional depth) and P7 (index line) applied directly — all `removes_content: false`. P1–P4, P8, P9 are doc-edits on the governance surface and await Sammy**: P1+P2 (a matched pair — the claim-set unit is per-AGENT, `lenses + 2 × deduped findings` × ~70–90k, and the §5 table row that currently outranks it), P3 (the L-minimum exemption for E8-S1/E3-S1/E6-S1 is false — a scope decision on two un-run stories), P4 (`run-story` §5: read the pass cost at the dedup barrier, before the skeptic wave), P8 (the roadmap's #509 record is Anthropic-only and E8-S2 reads it), P9 (the design-story variant is defined twice and each copy is right about one half — charter says steps 3–5 and step 8 n/a, skill says 4–6 and step 9 n/a; the charter's range preserves QA, the skill's n/a is correct). P4 and P9 change how the team works, so charter §4.4 wants a design-mode red-team first. Postmortem also withdrew one drafted proposal after checking that `c966d8e6` had already landed the fix. **CURATE counter: exit #3** since the first CURATE row (E9-S7) — E9-S9 parked #1, E9-S9 gate-check blocked #2 — established by the §10c reconciliation, which is what caught that the gate-check run was a separate exit. |

| 2026-09-04 | E8-S1 follow-up (to the row above) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — / ~10.17M (3 design red-team passes over the governance change set: 0.66M held + 6.50M + 3.01M) / — / — | Sammy adjudicated the postmortem's nine proposals **one at a time** and then said **"merge both"**. Merged on his explicit go after the governance trigger fired, as designed: [#510](https://github.com/sammygallo/goodgirlsbotclub/pull/510) → `49a8f69d` and [#511](https://github.com/sammygallo/goodgirlsbotclub/pull/511) → `1510647a`, CI green on both heads (checks actually fired, 59s and 1m4s), branches deleted. **Decisions:** P1+P2 applied as proposed (the claim-set unit is priced per AGENT — `lenses + 2 × deduped findings` × ~70–90k — replacing a flat per-pass figure whose ceiling E8-S1 round 1 falsified at ~10.3M); P3 resolved **per story rather than as a blanket** (E3-S1 stays on charter §3's design-story roster and bands L, E6-S1 comes off and keeps its build band, and the roster and band now change together); P4 **scripted rather than written as prose**, on the tree-hygiene precedent; P9 single-sourced into the skill; P8 folded in; and the user-guide caveat **declined in favour of scheduling the fix as a story** — new card **E9-S10** for #509. P5/P6/P7 (memory) were applied directly during the park. **The gate reviewed itself and found itself unsafe.** Round 1 HELD before its own skeptic wave (4 lenses, 47 deduped, 98 agents projected at 7.84M against a 6M budget — 0.66M spent instead of 7.84M), and its unverified lens output showed, on PM verification against the code, that a held return carried no `confirmed` key: §8 merge-checklist item 3 ("the final review round confirmed zero findings") read TRUE off a pass that verified nothing, so a budget hold could have laundered an unverified trigger-tier diff into a mergeable one — **authority-widening introduced by the change §8's governance trigger exists to catch**. Fixed by making the four verdict keys explicitly `null` (a downstream count throws instead of reading zero), renaming the lens output `unverifiedFindings`, and stating in BOTH §8 item 3 and §5's lens-reduction bright line that a held run is not a review round — without the second, a hold satisfies "zero confirmed findings" by construction and would license a REDUCED lens set on the resumed pass, i.e. cost pressure shrinking a review, which roadmap §5 forbids in terms. **Each round then found a defect introduced by the previous round's fix — three times in the change set that prices exactly that failure mode:** round 2 (8 confirmed · 7 plausible · 21 refuted) found the stale test-case list and P9's fix inlining a copy of the roster it had just single-sourced; round 3 (4 confirmed · 3 plausible · 8 refuted) found round 2's input validation sitting BELOW the lens fan-out, so rejecting a mistyped budget first burned every lens agent (~320–400k, more than roadmap §5's whole standard-pass row) — both skeptics reproduced it by probe. Also fixed: a bare `7.16` meaning 7.16M passing every type check and silently disarming the cumulative half; armed and disarmed runs being indistinguishable in the record; per-pass rather than per-story pricing (which would not have fired on E8-S1's own round 2); the per-agent constant having THREE carriers, not the two the new lockstep instruction named; nothing invoking the tests, which `tree-hygiene.sh` and `deploy-notify-prs.sh` both already require; E3-S1's card reading Size M while §5 declared it L (the same card-vs-rule mismatch that cost E8-S1 ~18.3M, reproduced by the change set written to fix it); and E9-S10 citing the two-repo rule as established when it is a proposal pending approval (PR #464). **Gates:** `story-review.test.mjs` 31 assertions / 13 cases PASS, **eleven mutations verified red** across the session including reintroducing the exact reads-as-zero defect and moving validation back below the lens wave; skill-lint INTACT; tree-hygiene 8/8 and CLEAN. **Stopped at round 3 rather than running a fourth**: §5's Q2 read true on BOTH halves (three passes against a 1–2-pass class row; two or more fix rounds each introducing a new defect) and Sammy's authorization had covered the third pass only — presented, and he chose to merge. Converging, not converged: 47 → 36 → 15 raw findings, 8 → 4 confirmed. **These changes take effect for every future session in this repo immediately — no deploy is involved**, which is precisely why the governance trigger routes them to Sammy. Revert: `git revert 1510647a` and `git revert 49a8f69d`. |
| 2026-09-05 | `.pending` convention (bookkeeping, not a pipeline exit — does not count toward CURATE) | — | — | *(no exit; a governance PR arising from the E9-S9 / E9-S7 queue rescue recorded in the 2026-09-03 rows above)* | — / — / — / ~7.1M (design red-team of PR #512: 5 lenses × `adversarial-reviewer`, 36 findings × 2 refute-first verifiers, 78 agents, 0 errors; plus a 3-agent sweep, ~0.33M, that enumerated every stranded decision artifact across all 69 session scratchpads) | Sammy: **"open the governance PR"** then **"merge #512"** — merged as `89d7bbdc` on his explicit go (governance surface, three files; CI green on head `891afeef`; branch deleted). **The gate was fully specified except for the waiting:** nothing said where a `removes_content`/`memory-new` proposal lives between "needs Sammy's yes" and Sammy answering, so it lived in the session scratchpad. Shape merged: `SKILL.md` §10c gains the queue bullet — scoped to *every proposal still owed to Sammy, gated or additive-and-withheld*, files named `<story-id>-<YYYY-MM-DDTHHMMSS>-gated.md` and never overwritten, terminus **per proposal** (mark it decided, reconcile the README, delete the file only when nothing in it is owed), path in the ledger row, and a **bounded** `MEMORY.md` pointer (path + "read its README", never an inventory); the ledger bullet records that path beside the pre-image path; charter §5's gate row names the queue beside `DECLINED.md`; and `.claude/agents/postmortem.md` must read `.pending/` before proposing. **Red-team verdict: 12 upheld / 8 split / 16 refuted, and two of the four original edits would have misfired on the next run that obeyed them** — `<run-id>-gated.md` was undefined and non-unique against SKILL.md:131's own "a story can exit more than once" (E9-S9 exited twice on 2026-09-03 with `e9s9-gated.md` undecided), reproducing the exact collision the pre-image bullet two lines below was amended to fix; and the file-granular terminus was unfollowable against a queue whose files hold 3 / 60 / 201 independently-decidable items. Three false claims of the PM's were corrected in the same pass: "the pointer goes in it" (`MEMORY.md` is outside the repo), "silently dropped" (refuted 3/3 by row 49's own panel — the ledger indexes pending items by identity and that is how the rescue found them; what evaporates is the proposal *text*), and the `postmortem.md` exemplar (P6 and the queued CURATE entry are **opposite** edits on opposite sides of the gate, not one finding in two wordings, so the rule now says raise it, name the twin, decide together — as written it would have suppressed the stronger fix). **Ground truth corrected:** `/private/tmp/claude-501` content is swept within days, not merely lost at reboot (measured 2026-09-05 — oldest surviving file five days old, ~60 session dirs back to 2026-08-18 holding zero files), so `.pending/README.md`'s "not age-cleaned on this machine" was falsified and fixed in place with a pre-image (`.pre-image/pending-README.md.2026-09-05T120150.bak`); the panel's suggestion to propagate "dies at reboot" into SKILL.md:48 and :137 was declined as installing a new false claim. **Known non-compliance, gated on Sammy:** `MEMORY.md` line 3 (the queue pointer, added 2026-09-05 with pre-image `.pre-image/MEMORY.md.2026-09-05T112538.bak`) is 936 chars and duplicates the README inventory, which the merged bullet now forbids — shrinking it is a `removes_content` index rewrite, so it waits. **Open option, not required:** adding "snapshot to `.pre-image/` before deleting" to the terminus, which matters only if the 60-entry gated CURATE half is declined and its claim-level analysis would otherwise go. **Still with Sammy:** the whole `.pending/` queue — E9-S7's 261-claim CURATE batch, E9-S9's P2/P5/P6 — plus E9-S7's P8 rewrite, roadmap §5 SUBJECT term and the reviewer-stance drift PR. |
| 2026-09-05 | `.pending` convention follow-up (to the row above) | — | — | *(bookkeeping, not a pipeline exit — does not count toward CURATE)* | — / — / — / — | **The known non-compliance in the row above is closed.** Sammy: **"shrink the MEMORY.md pointer line"** — his yes on the `removes_content` index rewrite that row recorded as waiting. `MEMORY.md` line 3 shrunk 928 → 455 chars into the bounded form `89d7bbdc` requires: the path, **read its `README.md`**, why a reader should care (before a memory write, before the next CURATE, and the raise-the-twin rule), and no inventory — the counts and filenames it used to carry now live only in `.pending/README.md`, which the terminus reconciles item by item. Pre-image `.pre-image/MEMORY.md.2026-09-05T132627.bak` (captures the 928-char line; the earlier `…T112538.bak` captures the state before the pointer existed). Nothing else in the memory dir changed. |
| 2026-09-05 | E9-S7 CURATE batch decision (bookkeeping, not a pipeline exit — does not count toward CURATE) | — | — | *(no exit; Sammy delegated the batch with "decide the curate batch")* | — / — / — / ~19.4M (three passes: reconcile 106 agents ~9.9M, conservative trim 106 agents ~5.5M + ~3.6M lost to a usage-limit kill and resumed from cache, plus a 3-agent stranded-artifact sweep ~0.33M) | **127 of 261 claims settled; 134 left open and the run STOPPED rather than patched a third time.** **Remedy inverted mid-run, and that is the durable lesson:** pass 1 tried to make each stale claim true by REPLACING it with a freshly researched grounded one, and failed on its own terms — 33 of 52 files failed their preservation gate, and of the ten largest rewrites a critic sampled, FIVE had already introduced a new false claim (a shipment credited to the wrong story, three wrong `file:line` cites, a PR called open that had merged two minutes earlier); the memory index would have grown 56% on a file that loads into every session. Two of the new claims rotted DURING the run. Pass 2 inverted the doctrine — **make a stale claim true by DELETION; add no sha, PR number, line cite, date or count that is not already in the file** — with a mechanical gate on added specifics. That pass is what is live on the 26 settled files. **PROCESS FAILURE, and the most important line in this row: a trim agent wrote directly into the LIVE memory dir**, which its prompt forbade twice ("never write anywhere except your staged copy", "never modify anything in <memory-dir>"), overwriting 32 files with unreviewed content and bypassing the PM's pre-image step entirely. It was caught only because one gate agent noticed `diff LIVE TRIMMED` was empty and said so instead of passing. Recovery was possible only by luck: the 2026-09-02 CURATE pass had snapshotted `.pre-image/*.2026-09-02T101551.bak`, and pass 1's independently recorded `bytes_before` matched those snapshots byte-for-byte for all nine files in question, which is what made them verifiable restore points rather than guesses. Every live file was snapshotted to `.pre-image/RECOVERY-2026-09-05T162325/` before any repair. Seven files reverted to verified originals — the six whose trims failed the gate (`project_character_architecture_v2`, `project_character_creation_import`, `project_ggbc_intake_bot`, `project_multiuser_system`, `reference_local_test_env`, `reference_story_state_schema_v1` — real losses incl. 11 dropped claims in the schema doc and a reverted correction in the test-env doc) plus `project_roadmap_status`, whose live copy was an ungated first-attempt write the resume never replaced. **Stopped under §5 Q2** (`89d7bbdc`, merged this morning): two passes each introduced defects in the same seam is §8's *did not converge*, read early — so the 134 open claims are NOT to be patched a third time without Sammy's direction. `.pending/curate-DECISION-2026-09-05.md` records settled vs open per file and README.md points at it, per the terminus rule merged in the same commit. **P6 settled** — its delete-the-count remedy beat the batch's additive twin on the same finding (the ledger answers everything the clause asserted, and the additive text had itself gone stale within three days of being written); **P2 was applied by pass 1 though it was never in this batch, and was reverted with it**; P2 and P5 remain owed. |
| 2026-09-05 | E9-S7 CURATE batch CLOSED (bookkeeping, not a pipeline exit — does not count toward CURATE) | — | — | *(no exit; Sammy: "do the remaining 134" after the stop-and-present)* | — / — / — / ~4.6M this round (64 agents, 0 errors); ~24M across all three rounds | **All 261 claims decided; the queue is empty of this batch.** Round B took the 134 left open: **91 trimmed, 34 left standing** (verified not actually false — several were omissions, not falsehoods, and the batch's proposed fix was pure new-specific rot), **9 refuted** against today's ground truth. Five files needed one repair round; **none failed twice**, so the loop converged rather than being stopped again. **Two structural changes made this round work where two earlier ones failed.** (1) SANDBOX: agents were given COPIES under `scratchpad/curate3/orig` and never told where the live memory dir is — the one claim payload that leaked the path was scrubbed first. Last round exhortation failed and an agent overwrote 32 live files; this round `find -newermt` confirms zero live writes by any agent. (2) MINIMUM-SPAN doctrine, aimed at the actual round-B failure mode: deletion was right but six files had lost true facts sitting beside false ones, so the rule became cut the smallest span that makes the claim false, keep when unsure, and the gate hunted over-deletion specifically. Result: 0 over-deletions post-repair, 1 added specific (tense/hedge marking only, no sha/number/date), 1 still-false correctly deferred as a gated judgement call. Net −5,866 bytes on the 27 files; corpus 340,561 bytes. **A memory file that indicted this pipeline was itself wrong:** `feedback_github_actions_events_can_silently_not_fire` claimed two Actions events "never fired" on 2026-08-26; `gh api actions/runs` on both head shas shows both DID fire, 24 minutes and 7 minutes late. Same class as this session's own false 82-second claim. Its `wait_for_image` warning was also stale — `8e67ca31` (#473) fixed that helper — so the body and the MEMORY.md index line were both corrected to past tense. Pre-images: `.pre-image/PRE-CURATE3-2026-09-05T164335/` (27 files) on top of `RECOVERY-2026-09-05T162325/` (all 57). `.pending/curate-DECISION-2026-09-05.md` rewritten to DECIDED and README.md updated, per the terminus rule in `89d7bbdc`. **Still owed: P2 and P5 in `e9s9-gated.md`** — P5 is `memory-new`; P6 was settled in round A. |
| 2026-09-06 | E9-S9 P2 + P5 (bookkeeping, not a pipeline exit — does not count toward CURATE) | — | — | *(no exit; Sammy: "do p2 and p5")* | — / — / — / — | **P2 and P5 applied on Sammy's yes; the `.pending/` queue is now EMPTY.** Ground truth re-checked before writing (both drafts were three days old): ggbc-backend#84, #458, #506, #507 all OPEN; branch `claude/e9-s9-group-recall-identity` still `f892d731`; `ChatView`'s `lastGroupModeRef` and `deleteGroupChat`'s filter-only body unchanged on `main`; D-C9 survived the CURATE trim. **P2** → `feedback_adversarial_review_catches_real_bugs.md` §4 **row 19** (the E9-S9 datum), text as red-teamed 2026-09-03 with its two placeholders resolved to identifiers that now exist — the filed issues (#506/#507) and the fix-round rule's landing (`b773258b`, #508); index count 17x → 18x. **P5** → new `reference_group_chat_identity.md` (4,950 bytes; why a group chat's identity rests on three UI-owned inputs each wrong in some reachable state, and why no client-only fix works before backend#84) + one index line after the RAG/lorebook reference; the draft's two "issue filed, number in the ledger row" placeholders filled with #506/#507, its drifted-line-range example anchored to the date it was measured. Pre-images `.pre-image/MEMORY.md.2026-09-06T010739.bak` and `…feedback_adversarial_review_catches_real_bugs.md.2026-09-06T010739.bak`. **Terminus applied per `89d7bbdc`:** every item in `.pending/` was decided (P2, P5, P6, the 261 CURATE claims), so the five decided files left the directory — archived first to `.pre-image/PENDING-DECIDED-2026-09-06T*/` so the claim-level CURATE record survives (the red-team's optional snapshot-before-delete, adopted) — and README.md now reads as an empty queue with the convention and the decided history. MEMORY.md's bounded queue pointer stays true unchanged. **Everything the E9-S7 and E9-S9 postmortems owed Sammy is now closed** except the two that were never file-shaped: the roadmap §5 SUBJECT term and the reviewer-stance drift PR. |
| 2026-09-06 | E9-S7 escalations 1 + 2 closed (bookkeeping, not a pipeline exit — does not count toward CURATE) | — | — | *(no exit; Sammy: "do the SUBJECT term and the stance drift PR", then "merge #516 and #517")* | — / — / — / ~4.9M (one two-lane design red-team: 3 lenses × `adversarial-reviewer` per PR, 27 findings × 2 refute-first verifiers, 62 agents, 0 errors) | **The last two items the E9-S7 postmortem owed Sammy are merged on his explicit go: #516 → `fc52b0e9`, #517 → `c5612d64`** (governance surfaces; CI green on heads `6ce569c0` / `b4aa41a5`; branches deleted). **#516, roadmap §5 SUBJECT term:** the class-table paragraph now carries the term with `run-story` §5's definition — the rows price a story's DELIVERABLE, code or claim set; bookkeeping is not a deliverable, so a review over it is outside the table, not a class to add; E9-S7 (row 45) is the datum, E2-S5's #496 exit the in-table contrast. No calibration figure touched. Both no-shrink sentences self-tagged §6.1 (the trigger list, which never carried them): §5 → "restated in §6.6", §6.6 → §5. **Red-team 7 upheld / 2 split / 3 refuted — one blocker in the PM's own draft:** "every row above was priced on code diffs" was false (the claim-set row prices prose deliverables and says so) and would have misfired mechanically — a PM on a governance-prose story would find no class row, pass no `classBudgetTokens`, and #511's cost gate would run DISARMED on exactly the class E8-S1's overrun motivated it for. **#517, stance re-sync:** the constant gains the three `## Stance` items it lacked (lens scope, conditionalised for the shared skeptic prompt; safety-gate suspicion; refuting-your-own-candidate), the change-one-change-both note heads `## Stance` and names every mirrored section, test 31/31. **Red-team 7 upheld / 2 split / 6 refuted — nothing blocked on behaviour, but it reframed the item:** `story-review.js` has never passed an agent type, so the inline constant is the ONLY stance every lens and skeptic has ever received, in every session; it shipped condensed in `1a0b67c9` and lacked the three items from day one — not a later drift. The E9-S7 row named one missing bullet; the re-diff found three. Four prose claims of the PM's corrected against git and the ledger before merge. **Filed, not fixed:** charter line 67's claim that lens agents use the `adversarial-reviewer` agent type is false for the same reason → [#518](https://github.com/sammygallo/goodgirlsbotclub/issues/518), two honest fixes laid out for Sammy. **Correction to the 2026-09-06 P2+P5 row:** P8 (the four-word `(untracked in repo root)` rewrite) was NOT still owed — the CURATE trim had already removed the dead clause, so it closed by that pass, not by a decision. **Environment, not this run:** the shared checkout carries another session's uncommitted edits to nine `src/components/**` files (form-field `id`/`name`/`autoComplete` attributes; first seen 01:24, growing while this row was written) — left untouched, this commit staged by path; the hygiene gate refuses a snapshot over them until their owner commits or reverts. **Everything both postmortems owed Sammy is now closed.** |
| 2026-09-06 | #518 closed via #519 (bookkeeping, not a pipeline exit — does not count toward CURATE) | — | — | *(no exit; Sammy: "do #518", then "merge #519")* | — / — / — / ~2.0M (one design red-team: 3 lenses × `adversarial-reviewer`, 10 findings × 2 refute-first verifiers, 24 agents, 0 errors) | **#519 merged as `913e9cf1` on Sammy's explicit go; #518 CLOSED** (governance surface — charter + agent file; CI green on the amended head `9cb0e465`; branch deleted). **The choice, delegated and made:** fix the charter to describe the workflow rather than change the workflow to match the charter — the inline stance exists for a stated robustness reason, #517 had just re-synced it, `postmortem.md` §B5 already diffs the pair, and passing the agent type would be a behaviour change to every review for a line of prose. **Shape merged:** charter §4 line 67 now says lenses AND skeptics run on the `adversarial-reviewer` *stance*, condensed inline in `story-review.js`; its true half stays — lenses at opus/high, skeptics at high effort on the model the session resolves to (`:177` pins none), verifiers never economized — attributed to the script's options where it is set; the agent *type* is spawned only for one-off lenses outside the workflow, and charter-mandated design red-teams run `story-review` in design mode on the same stance. §3 line 44 and the §6 manifest row corrected to match; the agent file's own frontmatter ("Spawned by the story-review workflow") corrected in the same change — **the one edit that widened the diff to a second file, flagged in the body as Sammy's to strike; he merged it.** **Red-team 6 upheld / 2 split / 2 refuted → four defects, every one in the PM's own new text:** (1) "design red-teams" attributed to the agent type — charter :86/:237 and SKILL :145/:148 route them to design mode, 20 recorded design-mode runs were stance-carried vs 4 ad-hoc scripts on the type; (2) stance attached to lenses only, though the constant prefixes every skeptic prompt (`:176`); (3) "skeptics on the session's strongest model" — `:177` pins none, they inherit, switching the session model mid-review switches them; (4) the agent-file frontmatter contradiction the PR itself would have shipped. Two body nits (carrier overcount; "yesterday" was four minutes). **Repo fact learned:** `squash_merge_commit_message=COMMIT_MESSAGES`, so the durable copy of a governance PR's rationale is the HEAD COMMIT message, not the body — the head was amended and force-pushed with lease; earlier PRs' bodies were written on the opposite assumption. **Filed, not fixed:** `story-review.js:177`'s comment "skeptics inherit the strongest model" carries the same conflation → [#520](https://github.com/sammygallo/goodgirlsbotclub/issues/520) (comment fix recommended over pinning, which would cap skeptics below a Fable session). **Memory corrected at minimum span** (pre-image `.pre-image/feedback_workflow_empty_result_check_failures.md.2026-09-06T031707.bak`): the 2026-08-24 "verifiers died first" observation stands, its explanation did not — lenses are pinned to opus while skeptics inherit the session, so a limit on the session's tier takes out exactly the verification layer; no carrier of "strongest model" remains in the memory dir. **Open governance items with a card: #520 only.** |
| 2026-09-06 | #520 closed via #521 (bookkeeping, not a pipeline exit — does not count toward CURATE) | — | — | *(no exit; Sammy: "do #520", then "merge #521")* | — / — / — / ~0 (no agents: a one-line comment whose every claim is checkable against the adjacent code; verified by the PM directly, no design red-team — a deliberate, stated choice, below the bar §10c sets for process-rule changes) | **#521 merged as `3a32c90d` on Sammy's explicit go; #520 CLOSED** (governance surface `.claude/workflows/`; CI green on head `f8f1fa4c`; branch deleted). **Shape:** `story-review.js:177`'s comment no longer says skeptics "inherit the strongest model" — the call pins `effort: 'high'` and no model while the lens call (`:101`) pins `model: 'opus'`, so a skeptic inherits whatever the PM's session resolves to, switching the session model mid-review switches every skeptic while the lenses hold, and a limit on the session tier takes out the verification layer first (the 2026-08-24 incident). Behaviour unchanged; pinning rejected in #520 (caps skeptics below a Fable session, breaks the usage-limit workaround). `story-review.test.mjs` passes unchanged. Rationale carried in the head commit, per the squash setting learned on #519. **This closes the last governance item with a card.** The chain that ended here: E9-S7's postmortem (2026-09-02) → escalations 1+2 → #516/#517 → #517's red-team found #518 → #519 → #519's red-team found #520 → #521. Each hop was a pre-existing-and-adjacent carrier of a false claim found while verifying the previous fix, closed FILED-as-issue then fixed on Sammy's go — the closure `89d7bbdc` added, working as written. |
| 2026-09-06 | E9-S10 | deployed | CAPTURE | **The house's strongest review instrument is structurally blind to the defect class its own memory calls D-T4: `story-review`'s stance bar — "no failure scenario, no finding" — cannot produce a finding for a false docstring, and `system_placement.py` was NEW in ggbc-backend#87 with a module docstring claiming four post-history sections are "the last thing the model reads"; two trigger-tier rounds with full lenses and skeptics passed it, a standard-tier pass in the *other repo* caught it, and it cost follow-up PR #88 after merge. The run's dominant cost was one predicate, not nine rounds: loop 2 spent ~5.6M / 9 rounds against a ≤0.3M Standard row, four fix rounds each introducing a new defect, with over half the confirmed findings from rounds 2/3/4/6 and both QA passes being the same retracted absolute re-asserted in synonyms ("right before the AI responds" → "final" → "the last thing" → "closest possible spot" → "the spot it obeys most") until round 6 replaced the restatements with one canonical passage and rounds 7–9 converged. Loop 1, priced 6.2–18.5M, came in at ~2.7M — the band was on the wrong loop. And both edges of the deploy window moved after the request was presented: `main` grew by `9de118a5` (direct-to-`main`, no PR) and the droplet marker advanced `473c8aa2`→`9de118a5`, so the story was already live and had shipped with no release note — caught by re-reading, which §9 never tells anyone to do. E9-S9's Q1/Q2 fix-round trigger (`b773258b`) fired correctly on its first live test.** | ~0.85M / ~9.2M est. (12 rounds: loop 1 = 2 trigger-tier, 22 + 8 agents, ~2.7M; loop 2 = 9 standard, ~5.6M; #88 = 1, ~0.5M; QA ×3 ~0.4M) / ~8.2M est. (30 proposals → 29 upheld, 15 more from the critic, 54-hunk refute-first verification; ~3.4M of it lost to a usage-limit kill and recovered by resuming from the journal) / ~0.17M est. — PM main-loop tokens not included; the 2026-09-04 session's intake/plan/build are unrecorded (est. ~0.6M inside build) | Resumed on 2026-09-05 from a 2026-09-04 session's partial run (two clean worktrees, a PLAN board card `41a210db` carrying the mechanism and Sammy's unconditional-rollout decision; no PR, no row, spend unrecoverable). Backend [#87](https://github.com/sammygallo/ggbc-backend/pull/87) → `032de27` merged on Sammy's go after §8 item 3 read literally-not-met (round 2's two confirmed items were pre-existing, filed); docstring follow-up [#88](https://github.com/sammygallo/ggbc-backend/pull/88) → `fb26a80` self-serve; frontend docs+copy [#514](https://github.com/sammygallo/goodgirlsbotclub/pull/514) → `37154b0f` merged by Sammy at the did-not-converge gate (`run-story` §5's Q2 had fired at loop-2 round 3; Sammy: keep hardening). Deployed 2026-09-06 09:17Z on Sammy's go, re-presented once because `main` grew by `9de118a5` (direct-to-main, no PR) between request and go; frontend image `2412ed12`, backend `fb26a80`; the droplet's `.last-deployed` marker was already at `9de118a5` from another session's earlier deploy, so E9-S10 was live before this step and had no release note → posted to #feature-releases. Filed: ggbc-backend#85, ggbc-backend#86, [#513](https://github.com/sammygallo/goodgirlsbotclub/issues/513) (text-completion mode cannot produce a token on any provider — falsifies the working half of the pre-roadmap E9-S4 row), [#515](https://github.com/sammygallo/goodgirlsbotclub/issues/515). Plan absorption corrected the E9-S4 and #509 carriers across the roadmap (§1, §5, §7, E8 P0 gate), `ROADMAP.md` §3.5/§10.3, the audit reference, the tracked handoff doc, the board issues panel and four memory files. **Pre-images:** `memory/.pre-image/{MEMORY.md,reference_rag_lorebook_injection.md,project_roadmap_status.md,project_roadmap_v3_phases_10_2_to_12.md}.2026-09-06T054509.bak`, `{feedback_adversarial_review_catches_real_bugs.md,project_ggbc_dev_env.md}.2026-09-06T060224.bak`, `{MEMORY.md,reference_rag_lorebook_injection.md,project_roadmap_status.md}.2026-09-06T061130.bak`. Postmortem P4 (D-T7) and P5 (piped exit status) applied directly; P1–P3 (both stance carriers + `run-story` §9 re-derive-the-window) are doc-edits on branch `claude/e9-s10-postmortem` @ `0c2c8669`, design red-team in progress, PR to follow — nothing in `.pending/`. **Escalations:** (1) `project_ggbc_dev_env.md`'s recipe read `postgres:16-alpine` at this run's INTAKE (2026-09-05 11:16, erroring the whole suite at setup) and was corrected by another session later that day with no pre-image; (2) `MEMORY.md`'s "Confirmed 18x" now understates by one — bump vs delete at the next CURATE (E9-S9 P6 precedent); (3) `reference_ggbc_backend_test_running.md` still prescribes the compose `db` that `project_ggbc_dev_env.md` forbids — next CURATE; (4) the band sat on the wrong loop (loop 1 ~2.7M vs 6.2–18.5M; loop 2 ~5.6M vs ≤0.3M) — under #516's SUBJECT term loop 2 was a claim set, and a claim-set band would not have fired Q2 at round 3, the control that worked; (5) a code commit reached `main` with no PR. **CURATE count:** exit #4 since the 2026-09-02 CURATE row (E9-S9 parked, E9-S9 gate-check blocked, E8-S1 parked, this) — reconciled against `gh`: merged `claude/` PRs since then are #504 (rowed), the governance PRs #505/#508/#510/#511/#512/#516/#517/#519/#521 (rowed as bookkeeping), and this run's #514 + backend #87/#88; no unrowed exit. Infrastructure waste: ~3.4M absorption-verify agents killed by the usage limit; two `git push -q … | tail` non-pushes caught by reading the PR head. |
